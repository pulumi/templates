name: ${PROJECT}
description: ${DESCRIPTION}
runtime: go
template:
  description: A minimal Cloudflare Go Pulumi program
  config:
    accountId:
      description: The Cloudflare account ID to deploy into
