name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs

template:
  description: A TypeScript program to deploy a static website on Cloudflare
  config:
    accountId:
      description: The Cloudflare account ID to deploy into
    path:
      description: The path to the folder containing the website
      default: ./www
