name: ${PROJECT}
description: ${DESCRIPTION}
runtime: hcl
template:
  description: A Pulumi HCL program to deploy a web application onto a Kubernetes cluster
  config:
    k8s_namespace:
      description: The Kubernetes namespace to deploy into
      default: webapp
    num_replicas:
      description: The number of replicas to deploy
      default: 1
