name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: A minimal Linode JavaScript Pulumi program
  config:
    linode:project:
      description: The Linode project to deploy into
