name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: A Pulumi program to deploy a Helm chart onto a Kubernetes cluster
  config:
    k8sNamespace:
      default: nginx-ingress
      description: The Kubernetes namespace to deploy into
