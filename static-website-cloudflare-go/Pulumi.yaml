name: ${PROJECT}
description: ${DESCRIPTION}
runtime: go

template:
  description: A Go program to deploy a static website on Cloudflare
  config:
    accountId:
      description: The Cloudflare account ID to deploy into
    path:
      description: The path to the folder containing the website
      default: ./www
