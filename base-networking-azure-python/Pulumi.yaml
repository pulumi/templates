name: ${PROJECT}
description: ${DESCRIPTION}
runtime: python
template:
  description: A Python program to create a base networking setup on Azure
  config:
    azure-native:location:
      default: westus2
      description: The Azure region in which to create the infrastructure
