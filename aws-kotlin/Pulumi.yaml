name: ${PROJECT}
description: ${DESCRIPTION}
runtime: java
template:
  description: A minimal AWS Kotlin Pulumi program
  important: true
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
