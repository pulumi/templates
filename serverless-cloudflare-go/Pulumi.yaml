name: ${PROJECT}
description: ${DESCRIPTION}
runtime: go

template:
  description: A Go program to deploy a serverless application on Cloudflare
  config:
    accountId:
      description: The Cloudflare account ID to deploy into
