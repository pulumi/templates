name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: A minimal Cloudflare TypeScript Pulumi program
  config:
    accountId:
      description: The Cloudflare account ID to deploy into
