name: ${PROJECT}
description: ${DESCRIPTION}
runtime: hcl
template:
  description: A minimal AWS Pulumi HCL program
  important: true
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
