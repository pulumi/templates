name: ${PROJECT}
description: ${DESCRIPTION}
runtime: go
template:
  description: A minimal Azure Go Pulumi program
  config:
    azure:environment:
      description: The Azure environment to use. Supported values are `public`, `usgovernment`, `german`, `china`.
      default: public
