name: ${PROJECT}
description: ${DESCRIPTION}
runtime: hcl
template:
  description: A Pulumi HCL program to deploy a serverless application on Cloudflare
  config:
    account_id:
      description: The Cloudflare account ID to deploy into
