name: ${PROJECT}
description: ${DESCRIPTION}
runtime: bun
template:
  description: A minimal AWS Bun Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
