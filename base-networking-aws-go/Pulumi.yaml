name: ${PROJECT}
description: ${DESCRIPTION}
runtime: go
template:
  description: A Go program to create a base networking setup on AWS
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
    vpcNetworkCidr:
      description: Network CIDR to use for new VPC
      default: 10.0.0.0/16
