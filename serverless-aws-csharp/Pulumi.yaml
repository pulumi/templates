name: ${PROJECT}
description: ${DESCRIPTION}
runtime: dotnet
template:
  displayName: Serverless Application
  description: A C# program to deploy a serverless application on AWS
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
  metadata:
    cloud: aws
