name: ${PROJECT}
description: ${DESCRIPTION}
runtime: java
template:
  description: A minimal Azure Kotlin Pulumi program
  important: true
  config:
    azure-native:location:
      description: The Azure location to deploy into
      default: EastUS
